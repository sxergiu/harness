node_modules/
dist/
*.log
.DS_Store

# Per-machine permission grants, written by Claude Code as you approve things.
# It accumulates absolute scratchpad paths, session uuids and the names of
# whatever other checkouts you happened to copy a file from — none of which
# means anything on another machine, and all of which would ship in the first
# commit of a public repo.
.claude/settings.local.json
